<nav id="topbar" class="navbar navbar-expand-lg navbar-dark dark-purple border-bottom">
  <div class="container-fluid">

    <%= link_to root_path, class: "navbar-brand dark-purple inkind-logo" do %>
      <%= t(:title) %>
    <% end %>

    <div class="collapse navbar-collapse">
      <ul class="navbar-nav">
        <li class="nav-item nav-link">
          <a class="align-self-center hamburger-menu" href="#">
            <%= render inline: Rails.root.join('app', 'assets', 'images', 'icons', 'line_horizontal_3_20_regular.svg').read %>
          </a>
        </li>
      </ul>
    </div>

    <div>
      <ul class="navbar-nav">
        <% if user_signed_in? %>
          <li class="nav-item align-self-center">
            <a class="nav-link" href="#">
              <%= render inline: Rails.root.join('app', 'assets', 'images', 'icons', 'alert_20_filled.svg').read %>
            </a>
          </li>

          <li class="nav-item dropdown">
            <a class="nav-link" id="navbarDropdownMenu" href="#" role="button" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <!-- <%= image_tag current_user_avatar_url, class: "avatar" %> -->
              <div id="profile-circle" class="text-center">
                <div class="circle-text"><%= current_user.initials %></div>
              </div>
            </a>
            <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdownMenu">
              <%= link_to(t(".user_menu.edit_profile"), edit_user_registration_path, class: "dropdown-item" ) %>
              <div class="dropdown-divider"></div>
              <%= link_to(t(".admin_menu.edit_organization"), edit_admin_organization_path(1), class: "dropdown-item" ) %>
              <div class="dropdown-divider"></div>
              <%= link_to(t(".user_menu.logout"), destroy_user_session_path, class: "dropdown-item" ) %>
              </div>
            </div>
          </li>
        <% else %>
          <li class="nav-item dropdown">
            <%= link_to(t(".user_menu.login"), new_user_session_path, class: "nav-link" ) %>
          </li>
        <% end %>
      </ul>

    </div>
  </div>
</nav>
